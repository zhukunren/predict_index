[pytest]
testpaths = tests
pythonpath = .
addopts = --basetemp=.pytest_basetemp -m "not research_artifacts"
markers =
    research_artifacts: requires separately archived immutable research results; run explicitly with -m research_artifacts
